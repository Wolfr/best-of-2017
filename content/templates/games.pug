extends index

block append pageVariables
    - var pageTitle = "Games"

block maincontent
    each item in contentData.games.records
        .cover
            if item.fields.name
                .cover-image
                    +image(item)
                .cover-info
                    h3.cover-heading
                        | #{item.fields.name} 
                    p.meta
                        | #{item.fields.platforms} • #{moment(item.fields.releaseDate).format("MMMM Do")}
                    div!= marked(item.fields.review)

    .categories-nav
        a.btn(href="/index.html") Back to front page
    